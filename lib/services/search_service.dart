class SearchService {
  Future searchProduct(String query) async {
    // final response = await _supabaseClient
    //     .from('Products')
    //     .select()
    //     .textSearch("name", "'$query'");
    List responseList = [];
    return responseList;
  }
}
